class Bee extends Grub {
  //Constructor takes in new things or super things
  constructor() {
    //lets you access the object you're extending from
    //super takes in the arguments that its constructor takes in
    super();
    this.age = 5
    this.color = 'yellow'
    this.job = 'Keep on growing'
  }
};
